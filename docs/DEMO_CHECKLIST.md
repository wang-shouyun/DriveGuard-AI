# Demo Checklist

- 运行 `python scripts\smoke_test.py`。
- 检查 `assets/audio/*.wav` 是否存在。
- 启动程序后先进入模拟模式。
- 观察四级状态、趋势图和事件中心。
- 确认事件不会连续刷屏。
- 导出 HTML 报告并单独打开。
- 摄像头模式前关闭会议软件并检查系统权限。
- 摄像头不可用时使用模拟模式和视频文件模式作为备用。
- 答辩前清理或备份 `runtime/`，不要提交真实人脸数据。
